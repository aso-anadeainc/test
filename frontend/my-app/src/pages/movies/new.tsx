import MovieForm from '@/app/components/MovieForm';
import Layout from '@/app/features/Layout';

const NewMovie = () => {
	return <Layout pageTitle='Create a new movie'><MovieForm /></Layout	>
};

export default NewMovie;
